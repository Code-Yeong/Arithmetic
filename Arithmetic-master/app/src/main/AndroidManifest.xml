<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.administrator.arithmetic_master">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.LoginActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".view.SignupActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".view.GradeActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".view.MainPageActivity"
            android:theme="@style/AppTheme.Dark" >

        </activity>

        <activity android:name=".view.CalculateActivity"/>
        <activity android:name=".view.DoWrongActivity" />
        <activity android:name=".view.UserOrderActivity" />
        <activity android:name=".view.StatisticActivity" />
    </application>

</manifest>